[data]
vis = ''

[fields]
bpassfield = ''
fluxfield = ''
phasecalfield = ''
targetfields = ''
extrafields = ''

[slurm]                           # See processMeerKAT.py -h for documentation
nodes = 1
ntasks_per_node = 8
plane = 1
mem = 32                          # Use this many GB of memory (per node) for single-task scripts; jobs whose own parallelism already earns more (e.g. imaging/flagging) aren't capped by this. Only selfcal_part1 always uses the full node.
partition = 'Main'                # SLURM partition to use
exclude = ''                      # SLURM nodes to exclude
time = '12:00:00'
submit = False
container = '/software/projects/pawsey1164/ssankar/containers/idianext.sif'
mpi_wrapper = 'srun'
name = ''
dependencies = ''
account = ''
reservation = ''
modules = ['singularity/4.1.0-mpi']
verbose = False
precal_scripts = [('calc_refant.py',False,''), ('partition.py',True,'')]
postcal_scripts = [('concat.py',False,''), ('plotcal_spw.py', False, ''), ('selfcal_part1.py',True,''), ('selfcal_part2.py',False,''), ('run_sofia.py', False, '/software/projects/pawsey1164/ssankar/containers/SoFiA-V2.6.7-2025-03-12.sif'), ('uvsub.py', False, ''), ('uvcontsub.py', True, ''), ('hi_image.py', False, ''), ('hi_sofia.py', False, '/software/projects/pawsey1164/ssankar/containers/SoFiA-V2.6.7-2025-03-12.sif'), ('science_image.py', False, ''), ('cont_sofia.py', False, '/software/projects/pawsey1164/ssankar/containers/SoFiA-V2.6.7-2025-03-12.sif')]
scripts = [ ('validate_input.py',False,''),
            ('flag_round_1.py',True,''),
            ('calc_refant.py',False,''),
            ('setjy.py',True,''),
            ('xx_yy_solve.py',False,''),
            ('xx_yy_apply.py',True,''),
            ('flag_round_2.py',True,''),
            ('xx_yy_solve.py',False,''),
            ('xx_yy_apply.py',True,''),
            ('split.py',True,''),
            ('quick_tclean.py',True,'')]

[cluster]                         # Setonix hardware facts and named partitions -- see processMeerKAT.py -h for documentation. Not stamped from CLI args like [slurm] is; hand-edit if your allocation's partition names/limits ever differ.
total_nodes_limit = 1592          # Max nodes [-N --nodes] may request
cpus_per_node = 128               # Physical cores per node (256 logical/SMT threads, but CASA/tclean's numerically-bound work rarely benefits from hyperthreading)
mem_per_node_gb = 230             # Usable memory (GB) per node on the shared 'work'/'long' partitions
mem_per_node_gb_highmem = 1508    # Usable memory (GB) per node on the 'HighMem' partition
mem_per_cpu_mb_shared = 1840      # Memory (MB) per core SLURM allows on shared (non-exclusive) partitions -- requesting more than this ratio allows makes sbatch reject the job outright
default_mem_gb = 32               # Default [-m --mem] starting point for jobs sharing a node
default_partition = 'work'        # Default SLURM partition
long_partition = 'long'           # Partition used for long_partition-flagged scripts (see script_registry.py) -- longer walltime cap, far fewer nodes
highmem_partition = 'HighMem'     # Partition for jobs needing more than mem_per_node_gb
devel_partition = 'Devel'         # Partition used for short interactive/utility jobs (e.g. sky model queries, cleanup)

[crosscal]
minbaselines = 4                  # Minimum number of baselines to use while calibrating
chanbin = 1                       # Number of channels to average before calibration (during partition)
width = 1                         # Number of channels to (further) average after calibration (during split)
timeavg = '8s'                    # Time interval to average after calibration (during split)
createmms = True                  # Create MMS (True) or MS (False) for cross-calibration during partition
keepmms = True                    # Output MMS (True) or MS (False) during split
spw = '*:880~933MHz,*:960~1010MHz,*:1010~1060MHz,*:1060~1110MHz,*:1110~1163MHz,*:1299~1350MHz,*:1350~1400MHz,*:1400~1450MHz,*:1450~1500MHz,*:1500~1524MHz,*:1630~1680MHz' # Spectral window / frequencies to extract for MMS
nspw = 11                         # Number of spectral windows to split into
calcrefant = False                # Calculate reference antenna in program (overwrites 'refant')
refant = 'm059'                   # Reference antenna name / number
standard = 'Stevens-Reynolds 2016'# Flux density standard for setjy
badants = []                      # List of bad antenna numbers (to flag)
badfreqranges = [ '933~960MHz',   # List of bad frequency ranges (to flag)
                  '1163~1299MHz',
                  '1524~1630MHz']

[selfcal]
# Self-calibration stage list: one dict per loop (including loop 0, the initial dirty
# image); nloops is derived as len(stages)-1, not configured separately. 'mask'/
# 'apply_cal' are relative references -- None (don't use one) or 'prev' (use the
# immediately preceding stage's mask / derived calibration). 'derive_cal' is the gaincal
# calmode to solve for from this stage's image ('' to skip solving, 'p' for phase-only,
# 'ap' for amplitude+phase). 'threshold' is a S/N value if >= 1.0, otherwise a CASA
# quantity string (e.g. '0.5mJy'); 'solint' only matters when 'derive_cal' != ''. To
# lengthen the self-cal chain, append another stage dict -- no other keys need editing.
stages = [{'mask': None, 'apply_cal': None, 'derive_cal': '', 'niter': 10000, 'threshold': '0.5mJy'},
          {'mask': 'prev', 'apply_cal': None, 'derive_cal': 'p', 'niter': 50000, 'threshold': 10, 'solint': '1min'},
          {'mask': 'prev', 'apply_cal': 'prev', 'derive_cal': 'ap', 'niter': 80000, 'threshold': 5, 'solint': '10min'},
          {'mask': 'prev', 'apply_cal': 'prev', 'derive_cal': '', 'niter': 1000000, 'threshold': 3}]
loop = 0                          # If nonzero, adds this number to nloops to name images or continue previous run
cell = '1.5arcsec'
robust = -0.5
imsize = [6144, 6144]
wprojplanes = 512
nterms = 1                        # Number of taylor terms
gridder = 'wproject'
deconvolver = 'multiscale'
scales = [0,5,10,15]
usermask = ''
uvrange = ''                      # uv range cutoff for gaincal
flag = True                       # Flag residual column after selfcal?
gaintype = 'G'                    # Use 'T' for polarisation on linear feeds (e.g. MeerKAT)
discard_nloops = 0                # Discard this many selfcal solutions (e.g. from quick and dirty image) during subsequent loops (only considers when derive_cal != '')
outlier_threshold = 0.0           # S/N values if >= 1.0, otherwise Jy
outlier_radius = 0.0              # Radius in degrees for identifying outliers in RACS
atrous_do = True
flag_maxsize_bm = 30.0
pb_correct = False                # Apply katbeam PB-correction (image_engine.do_pb_corr(), shared with [cont_image]/[hi_image]) to the final loop's own image? Off by default -- see REFACTOR_PLAN.md's Phase 6 write-up
pbthreshold = 0.1                 # Threshold below which to mask the PB for PB correction
pbband = 'LBand'                  # Which band to use to generate the PB - one of "LBand" "SBand" or "UHF"

[cont_image]                      # Continuum imaging (was [image]/science_image.py's fixed single-pass; Phase 6 of the Pawsey refactor made this stage-list-driven, matching [hi_image]'s shape, and renamed to make explicit it's the continuum-specific counterpart to [hi_image])
vis = ''                          # Path to MS to image. '' means use this run's own [data] vis (today's behaviour) -- set explicitly to image an independently-provided/concatenated MS instead.
# Continuum imaging stage list: one dict per stage. 'mask'=None or 'prev' (SoFiA island
# mask from the immediately preceding stage's image). The last stage is implicitly
# "final": gets optional rebin/PB-correction/export, then one final SoFiA source-finding
# pass; every earlier stage gets a plain SoFiA masking pass feeding the next stage.
stages = [{'mask': None, 'niter': 10000, 'threshold': '1mJy'},
          {'mask': 'prev', 'niter': 50000, 'threshold': '0.2mJy'}]
cell = '2arcsec'
imsize = [2048, 2048]
robust = 0.0
uvtaper = ''
scales = [0, 5, 10, 15]
gridder = 'wproject'
wprojplanes = 128
deconvolver = 'multiscale'
weighting = 'briggs'
nterms = 2                        # Number of taylor terms (use deconvolver='mtmfs' for a true multi-term continuum fit)
specmode = 'mfs'                  # 'mfs' or 'mtmfs' for continuum -- HI cube imaging is the separate [hi_image] pipeline
restfreq = '1420.406MHz'          # HI line (change for other emission lines - e.g. OH) -- [hi_image] has its own separate 'restfreq', not read from here
restoringbeam = ''
stokes = 'I'
rebin = False                     # Rebin the final stage's image before export?
rebin_factor = [2, 2, 1]          # imrebin() factor, only used when rebin=True
pb_correct = False                # Apply katbeam PB-correction to the final stage's image before export?
pbthreshold = 0.1                 # Threshold below which to mask the PB for PB correction
pbband = 'LBand'                  # Which band to use to generate the PB - one of "LBand" "SBand" or "UHF"
outlierfile = ''
# SoFiA parameter overrides applied on top of the shared default_hi_sofmask.txt base
# template at runtime (sofia_engine.run_pass()) -- one dict for the masking pass (every
# non-final stage), one for the final source-finding pass (the last stage). Key namespace
# is SoFiA's own dotted parameter-file format, not this pipeline's.
sofia_mask_params = {'scfind.kernelsXY': '0, 4, 8', 'reliability.threshold': 0.9,
                      'output.writeCatASCII': False, 'output.writeMoments': False,
                      'output.writeCubelets': False, 'output.writePV': False}
sofia_final_params = {'scfind.kernelsXY': '0, 4, 8, 12', 'reliability.threshold': 0.95,
                       'output.writeCatASCII': True, 'output.writeMoments': True,
                       'output.writeCubelets': True, 'output.writePV': True}
combo = 0                         # Internal: unused for [cont_image] (no combo axis, unlike [hi_image]) -- reserved for consistency
stage = 0                         # Internal: current stage index in 'stages', advanced by cont_sofia.py

[contsub]                         # uvcontsub.py's continuum-fit parameters -- shared by -H and standalone --contsub, independent of whether continuum imaging ([cont_image]) itself runs
fitspw = ''                       # Emission line frequencies to exclude from fit to continuum during uvcontsub
fitorder = 1                      # Order of polynomial fit to continuum during uvcontsub

[hi_image]                        # HI (spectral-line) cube imaging -- Phase 6 of the Pawsey refactor (see REFACTOR_PLAN.md). Images [run] hi_contsub_vis (uvcontsub.py's output, gated on -H/--contsub), not [data] vis.
# Weighting combinations to image, one dict per combination -- the *entire* stage chain
# below runs independently for each entry (no mask-sharing across combos). Compare
# sensitivity/resolution trade-offs by adding more entries.
hi_combos = [{'robust': 1.0, 'uvtaper': ''}]
# HI cube imaging stage list -- same shape/semantics as [cont_image]'s 'stages' (see its
# comment above). Defaults ported from the m2-image-scripts prototype (origin/HI-dev).
stages = [{'mask': None, 'niter': 50000, 'threshold': '0.6mJy'},
          {'mask': 'prev', 'niter': 1500000, 'threshold': '0.24mJy'}]
cell = '2.0arcsec'
imsize = [2048, 2048]
scales = [0, 3, 5, 10]
gridder = 'wproject'
wprojplanes = 256
deconvolver = 'multiscale'
weighting = 'briggs'
restfreq = '1420.406MHz'           # HI line (change for other emission lines - e.g. OH) -- own copy, not read from [cont_image]
imspw = ''                        # SPW to select for imaging -- own copy, not read from [cont_image]
rebin = False                     # Rebin the final stage's cube before export?
rebin_factor = [2, 2, 1]          # imrebin() factor, only used when rebin=True
pb_correct = False                # Apply katbeam PB-correction to the final stage's cube before export?
pbthreshold = 0.1                 # Threshold below which to mask the PB for PB correction
pbband = 'LBand'                  # Which band to use to generate the PB - one of "LBand" "SBand" or "UHF"
# SoFiA parameter overrides applied on top of the shared default_hi_sofmask.txt base
# template at runtime (sofia_engine.run_pass()) -- one dict for the masking pass (every
# non-final stage), one for the final source-finding pass (the last stage). Key namespace
# is SoFiA's own dotted parameter-file format, not this pipeline's.
sofia_mask_params = {'scfind.kernelsXY': '0, 4, 8', 'reliability.threshold': 0.9,
                      'output.writeCatASCII': False, 'output.writeMoments': False,
                      'output.writeCubelets': False, 'output.writePV': False}
sofia_final_params = {'scfind.kernelsXY': '0, 4, 8, 12', 'reliability.threshold': 0.95,
                       'output.writeCatASCII': True, 'output.writeMoments': True,
                       'output.writeCubelets': True, 'output.writePV': True}
combo = 0                         # Internal: current index into 'hi_combos', advanced by hi_sofia.py
stage = 0                         # Internal: current stage index in 'stages' (reset to 0 when 'combo' advances), advanced by hi_sofia.py

[run]                             # Internal variables for pipeline execution
continue = True
dopol = False
